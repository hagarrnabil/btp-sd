{
  "xsappname": "btp-sd-v2",
  "tenant-mode": "dedicated",
  "oauth2-configuration": {
    "credential-types": ["x509"]
  },
  "scopes": [
    {
      "name": "$XSAPPNAME.ReadAll",
      "description": "Full read permissions including invoices and sales orders."
    },
    {
      "name": "$XSAPPNAME.ReadAllExceptTotal",
      "description": "Read permissions excluding total amount in invoices."
    }
  ],
  "role-templates": [
    {
      "name": "ReadAll",
      "description": "Full read access to all data.",
      "scope-references": [
        "$XSAPPNAME.ReadAll"
      ]
    },
    {
      "name": "ReadAllExceptTotal",
      "description": "Read access to all data except total amount in invoices.",
      "scope-references": [
        "$XSAPPNAME.ReadAllExceptTotal"
      ]
    }
  ],
  "role-collections": [
    {
      "name": "Read-All",
      "description": "Role for full read access.",
      "role-template-references": [
        "$XSAPPNAME.ReadAll"
      ]
    },
    {
      "name": "Read-All-Except-Total",
      "description": "Role for read access excluding total amount in invoices.",
      "role-template-references": [
        "$XSAPPNAME.ReadAllExceptTotal"
      ]
    }
  ]
}
