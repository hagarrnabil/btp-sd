package com.example.btpsd.repositories;

import com.example.btpsd.model.dCloud;
import org.springframework.data.jpa.repository.JpaRepository;

public interface dCloudRepository extends JpaRepository<dCloud, Long> {
}
